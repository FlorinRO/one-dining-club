# Expo reads EXPO_PUBLIC_* vars at bundle time.
# For physical devices on LAN, point to your Mac IP (not localhost).
EXPO_PUBLIC_API_URL=http://192.168.0.141:8000/api

# Optional production override (used when not in __DEV__).
EXPO_PUBLIC_PRODUCTION_API_URL=https://api.onedining.club/api
